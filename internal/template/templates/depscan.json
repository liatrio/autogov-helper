{
  "scanner": {
    "uri": "{{ .ScannerURI }}",
    "version": "{{ .ScannerVersion }}",
    "db": {
      "uri": "https://toolbox-data.anchore.io/grype/databases/listing.json",
      "version": "{{ .DBVersion }}",
      "lastUpdate": "{{ .DBLastUpdate }}"
    },
    "result": {{ .Results }}
  },
  "metadata": {
    "scanStartedOn": "{{ .DBLastUpdate }}",
    "scanFinishedOn": "{{ .Created }}"
  }
} 