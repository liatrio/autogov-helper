{
  "scanner": {
    "uri": "{{ .ScannerURI }}",
    "version": "{{ .ScannerVersion }}",
    "db": {
      "name": "{{ .DBName }}",
      "version": "{{ .DBVersion }}",
      "lastUpdated": "{{ .DBLastUpdated }}"
    },
    "result": [
      {
        "id": "{{ .VulnID }}",
        "severity": {
          "method": "{{ .SeverityMethod }}",
          "score": "{{ .SeverityScore }}"
        }
      }
    ]
  }
} 